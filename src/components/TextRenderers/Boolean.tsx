import React from "react"

import { TextRenderer, renderWithInfo} from "./TextRenderer"
import { Text } from "./BasicText"

import { _get, _hasOwnProperty, _isNull } from "@common/utils";

export const Boolean = <T,>({ props }: TextRenderer<T>) => {

}